import { CameraIcon } from '@heroicons/react/24/outline'
import { registerElement } from '../metadata'

registerElement({
  key: 'shotType',
  label: 'Shot Type',
  icon: CameraIcon,
  description: 'Camera framing and composition',
  group: 'composition'
})
