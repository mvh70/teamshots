import { SparklesIcon } from '@heroicons/react/24/outline'
import { registerElement } from '../metadata'

registerElement({
    key: 'preset',
    label: 'Preset',
    icon: SparklesIcon,
    description: 'Choose a professional shot preset optimized for specific use cases',
    group: 'composition',
})
